# Test Generation Agent Toolkit 操作手册

> 基于 FastAPI + LangGraph 的自动化测试用例生成 Agent，同时集成了 TestPilot v3.x 企业级前端。

---

## 目录

- [一、系统概述](#一系统概述)
- [二、环境要求](#二环境要求)
- [三、安装部署](#三安装部署)
- [四、配置说明](#四配置说明)
- [五、快速上手](#五快速上手)
- [六、Web 控制台使用指南](#六web-控制台使用指南)
- [七、CLI 命令行工具](#七cli-命令行工具)
- [八、核心功能详解](#八核心功能详解)
- [九、接口测试模块](#九接口测试模块)
- [十、测试洞察与数据管理](#十测试洞察与数据管理)
- [十一、项目扫描与批量生成](#十一项目扫描与批量生成)
- [十二、报告中心](#十二报告中心)
- [十三、异步任务与 WebSocket](#十三异步任务与websocket)
- [十四、Docker 沙箱隔离](#十四docker-沙箱隔离)
- [十五、性能测试引擎](#十五性能测试引擎)
- [十六、前端开发与构建](#十六前端开发与构建)
- [十七、测试与质量保障](#十七测试与质量保障)
- [十八、CI/CD 与 Docker 部署](#十八cicd与docker部署)
- [十九、常见问题排查（FAQ）](#十九常见问题排查faq)
- [二十、API 参考](#二十api参考)

---

## 一、系统概述

本项目是一个 **自动化测试用例生成与测试管理平台**，融合了两大核心能力：

1. **AI 驱动的测试用例生成** — 基于 LangGraph 工作流，自动分析源代码并生成可运行的 pytest 测试。
2. **企业级测试管理** — 集成了 TestPilot v3.x 前端，提供用例管理、接口测试、缺陷跟踪、报告中心等完整测试工作流。

### 核心架构

```
┌────────────────────────────────────────────────┐
│         Web 控制台（FastAPI + Vue3）            │
│  ├─ TestPilot 前端（/ms）                    │
│  ├─ 精简控制台（/）                            │
│  └─ REST API + WebSocket                       │
├────────────────────────────────────────────────┤
│            LangGraph 工作流                     │
│  scan_code → generate_mocks → generate_tests   │
│    → test_runner ⇄ refinement → coverage       │
│    → performance_test                          │
├────────────────────────────────────────────────┤
│           服务支撑层                            │
│  ├─ Docker 沙箱（安全隔离测试执行）             │
│  ├─ 异步任务队列                                │
│  ├─ SQLite 持久化（tga.db）                    │
│  └─ LLM Provider（OpenAI/Azure/本地）          │
└────────────────────────────────────────────────┘
```

### 支持的测试类型（7 种）

| 类型 | key | 说明 |
|------|-----|------|
| 功能测试 | `functional` | 验证业务功能正确性 |
| 接口测试 | `api` | 验证 API 请求/响应契约 |
| UI 测试 | `ui` | 验证用户界面元素与交互 |
| 性能测试 | `performance` | 验证响应时间、吞吐量与 SLO |
| 安全测试 | `security` | 验证注入、越权与敏感信息保护 |
| 兼容性测试 | `compatibility` | 跨版本/跨平台/跨配置兼容 |
| 可靠性测试 | `reliability` | 幂等性、容错性与稳定性 |

---

## 二、环境要求

### 2.1 硬件要求

| 组件 | 最低要求 | 推荐配置 |
|------|----------|----------|
| CPU | 2 核 | 4 核及以上 |
| 内存 | 4 GB | 8 GB 及以上 |
| 磁盘 | 5 GB | 20 GB（含 Docker 镜像缓存） |

### 2.2 软件要求

| 软件 | 版本要求 | 说明 |
|------|----------|------|
| Python | ≥ 3.10 | 建议 3.12 |
| Node.js | ≥ 18 | 前端开发需要 |
| Docker | ≥ 20.10 | 可选，用于测试沙箱隔离 |
| pip | 最新版 | Python 包管理 |

---

## 三、安装部署

### 3.1 从源码运行（开发环境）

#### 步骤 1：克隆代码

```bash
git clone <仓库地址> test-gen-agent
cd test-gen-agent
```

#### 步骤 2：安装后端依赖

```bash
pip install -r requirements.txt
```

#### 步骤 3：配置环境变量

```bash
cp .env.example .env
# 编辑 .env，填入 OPENAI_API_KEY（详见第四章）
```

#### 步骤 4：启动服务

```bash
# 方式一：直接使用 uvicorn
uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

# 方式二：使用开发脚本（推荐）
./scripts/dev-frontend.sh            # 同时启动前后端
./scripts/dev-frontend.sh backend    # 仅启动后端
./scripts/dev-frontend.sh frontend   # 仅启动前端
```

启动后访问：
- 精简控制台：`http://localhost:8000`
- TestPilot 前端：`http://localhost:5173`（开发模式）
- API 文档：`http://localhost:8000/docs`

### 3.2 Docker Compose 部署（前后端调试）

```bash
# 1. 配置环境变量
cp .env.example .env

# 2. 构建并启动
docker compose up --build -d

# 3. 查看日志
docker compose logs -f

# 4. 停止
docker compose down
```

访问地址：
- 前端（Vite dev server + HMR）：`http://localhost:5173`
- 后端（FastAPI 文档）：`http://localhost:8000/docs`

### 3.3 Docker 单容器部署

```bash
# 构建镜像（包含前端构建产物 + 后端运行时）
docker build -t test-gen-agent .

# 运行
docker run -p 8000:8000 \
  -e OPENAI_API_KEY=your-key \
  -v $(pwd)/data:/app/data \
  test-gen-agent
```

访问：
- 精简控制台：`http://localhost:8000`
- TestPilot 前端：`http://localhost:8000/ms`

---

## 四、配置说明

所有配置通过 `.env` 文件管理（复制自 `.env.example`）。配置使用 Pydantic Settings 自动加载并做类型校验。

### 4.1 LLM Provider 配置

```ini
# 可选: openai | azure | local（OpenAI 兼容端点）
LLM_PROVIDER=openai
LLM_MODEL=gpt-4o
LLM_TEMPERATURE=0.0
LLM_TIMEOUT=120
LLM_MAX_RETRIES=2

# OpenAI / OpenAI 兼容端点（必填）
OPENAI_API_KEY=sk-your-actual-key-here
# 可选：自定义 base_url（本地或兼容端点时配置）
# OPENAI_API_BASE=https://your-endpoint/api/v1
```

**支持三种 Provider：**

1. **OpenAI**：默认，直接使用 `OPENAI_API_KEY`
2. **Azure OpenAI**：配置 `AZURE_API_KEY`、`AZURE_ENDPOINT`、`AZURE_API_VERSION`
3. **本地兼容端点**（vLLM/Ollama/LiteLLM）：配置 `OPENAI_API_BASE` 指向本地端点，`OPENAI_API_KEY` 可填任意占位符

### 4.2 Agent 行为配置

```ini
MAX_RETRIES=3              # 测试修复最大重试次数
MAX_COVERAGE_RETRIES=5     # 覆盖率补测总重试上限
COVERAGE_THRESHOLD=80.0    # 覆盖率达标阈值(%)
TEST_TIMEOUT=60            # 单次 pytest 运行超时（秒）
```

### 4.3 服务配置

```ini
HOST=0.0.0.0               # 监听地址
PORT=8000                  # 监听端口
ENVIRONMENT=development    # development/staging/production
DEBUG=true                 # 生产环境建议 false
```

### 4.4 Docker 沙箱配置

```ini
SANDBOX_ENABLED=true       # 是否启用 Docker 沙箱隔离
SANDBOX_IMAGE=python:3.12-slim
SANDBOX_MEM_LIMIT=512m     # 容器内存限制
SANDBOX_NANO_CPUS=1000000000  # CPU 限制（1 CPU = 1e9）
SANDBOX_NETWORK_MODE=none  # 默认禁用网络（安全）
```

> **注意**：在本地调试（docker-compose）时建议设置 `SANDBOX_ENABLED=false`，避免 Docker-in-Docker 嵌套。

### 4.5 性能测试配置

```ini
PERF_ENABLED=false          # 是否在用例生成流程中默认执行性能测试
PERF_ITERATIONS=50          # 采样次数
PERF_WARMUP=5               # 预热次数
PERF_CONCURRENCY=1          # 并发线程数
PERF_P95_MAX_SEC=1.0        # P95 响应时间上限（秒）
PERF_THROUGHPUT_MIN=10.0    # 吞吐量下限（次/秒）
PERF_MEMORY_MAX_MB=512.0    # 峰值内存上限（MB）
```

### 4.6 LLM 限流重试

```ini
LLM_RATE_LIMIT_MAX_RETRIES=3      # 限流错误最大重试次数
LLM_RATE_LIMIT_BASE_DELAY=2.0     # 初始等待秒数
LLM_RATE_LIMIT_MAX_DELAY=30.0     # 最大等待秒数
LLM_RATE_LIMIT_BACKOFF_FACTOR=2.0 # 退避倍数
```

---

## 五、快速上手

### 5.1 场景一：单文件测试生成

```bash
# 生成默认功能测试
python -m app.cli generate --source-file examples/wechat_upload.py

# 指定测试类型生成
python -m app.cli generate --source-file examples/wechat_upload.py --test-type api
python -m app.cli generate --source-file examples/wechat_upload.py --test-type security
python -m app.cli generate --source-file examples/wechat_upload.py --test-type performance
```

生成结果：
- 测试文件输出到 `output/` 目录（如 `output/test_wechat_upload.py`）
- 控制台显示通过状态与覆盖率

### 5.2 场景二：项目级批量生成

```bash
python -m app.cli generate --project-path ./src --report
```

递归扫描 `./src` 目录下所有 Python 文件，批量生成测试并汇总报告。

### 5.3 场景三：仅生成结构化用例

```bash
# 不生成 pytest 脚本，仅生成结构化用例描述
python -m app.cli generate --source-file demo.py --no-script
```

### 5.4 场景四：Web 控制台操作

1. 启动服务后访问 `http://localhost:8000`
2. 在侧边栏选择「测试生成」
3. 粘贴源码或选择文件上传
4. 选择测试类型，点击「生成」
5. 实时查看生成进度与测试结果

---

## 六、Web 控制台使用指南

### 6.1 精简控制台（默认入口 `/`）

**顶部导航栏：**
- 工作空间切换
- 模块导航（仪表盘/测试生成/用例库/缺陷/项目扫描/报告/洞察/任务）

**侧边栏功能菜单：**

| 菜单项 | 图标 | 功能 |
|--------|------|------|
| 工作台 | 📊 | 用例统计、缺陷统计、最近活动、状态分布 |
| 测试生成 | ⚙️ | 单文件/项目批量/AI 生成 |
| 用例库 | 📚 | 用例 CRUD、搜索过滤、状态流转 |
| 缺陷跟踪 | 🐛 | 缺陷生命周期管理 |
| 项目扫描 | 📁 | 递归扫描项目目录、批量生成 |
| 报告中心 | 📊 | HTML/JUnit/Markdown 报告生成与下载 |
| 测试洞察 | 💡 | 价值量化、执行追溯、风险预警、低代码 |
| 任务队列 | ⚡ | 后台异步任务状态监控 |

### 6.2 TestPilot 前端（`/ms` 或 `:5173`）

登录后进入完整 TestPilot 界面，包含以下模块：

| 模块 | 路径 | 功能 |
|------|------|------|
| 工作台 | `/workstation` | 工作项待办、已关注、已创建 |
| AI 用例生成 | `/ai-case-gen` | 索引/低代码/项目三种生成模式 |
| 接口测试 | `/api-test` | 接口定义/用例/场景/报告/调试 |
| 用例管理 | `/case-management` | 功能用例/评审/回收站 |
| 缺陷管理 | `/bug-management` | 缺陷列表/详情/回收站 |
| 测试计划 | `/test-plan` | 测试计划列表/报告/配置 |
| 项目管理 | `/project-management` | 权限/模板/成员/版本 |
| 系统设置 | `/setting` | 用户/组织/参数/资源池/任务中心 |

---

## 七、CLI 命令行工具

### 7.1 命令概览

```bash
python -m app.cli <command> [options]
```

### 7.2 generate — 生成测试用例

```bash
python -m app.cli generate \
  --source-file <path>      # 单个源文件路径
  --project-path <dir>      # 项目目录路径（递归扫描）
  --output-dir output       # 输出目录（默认 output/）
  --test-type functional    # 测试类型
  --no-script               # 仅生成结构化用例
  --report                  # 生成报告
  --format html             # 报告格式：html/junit/markdown
```

**支持管道输入：**

```bash
cat my_code.py | python -m app.cli generate
```

### 7.3 list-cases — 列出用例

```bash
python -m app.cli list-cases
python -m app.cli list-cases --status approved
python -m app.cli list-cases --priority P0
python -m app.cli list-cases --json    # JSON 输出
```

### 7.4 defects — 列出缺陷

```bash
python -m app.cli defects
python -m app.cli defects --status open
python -m app.cli defects --severity blocker
python -m app.cli defects --json
```

---

## 八、核心功能详解

### 8.1 AI 测试生成工作流

系统使用 LangGraph 状态图驱动整个生成流程：

```
START
  → scan_code          (AST 提取函数签名)
  → generate_mocks     (规则驱动生成 Mock 配置)
  → generate_tests     (LLM 生成 pytest 测试)
  → test_runner        (子进程沙箱运行测试)
      ⇄ refinement     (失败时定向修复，条件重试)
  → coverage_analysis  (覆盖率差距分析)
      ⇄ refinement     (覆盖率不达标时可补测)
  → performance_test   (性能基准 + SLO 校验，可选)
  → END
```

**智能重试机制：**
- 测试失败时自动进入修复循环（最多 `MAX_RETRIES` 次）
- 覆盖率不达标时触发补测（最多 `MAX_COVERAGE_RETRIES` 次）
- 不可恢复的致命错误会跳过修复直接进入覆盖率分析

### 8.2 用例库管理

**用例状态流转：**
```
草稿(draft) → 评审中(review) → 已批准(approved) / 已废弃(deprecated)
```

**核心操作（Web 控制台）：**

| 操作 | 说明 |
|------|------|
| 创建用例 | 填写标题、描述、标签、优先级、测试类型 |
| 搜索过滤 | 按状态/优先级/标签/关键词过滤 |
| 脑图视图 | 树形结构展示用例层级 |
| 导入/导出 | Excel(CSV) / XMind(JSON) 格式 |
| 评审流程 | 提交评审 → 通过/驳回 |
| 依赖关系 | 前置/后置依赖管理 |
| 回收站 | 软删除 → 恢复/彻底删除 |
| 版本管理 | 自动快照、版本回滚 |
| 变更记录 | 完整审计日志 |
| 需求关联 | 关联 JIRA/TAPD 需求工单 |

### 8.3 缺陷跟踪

**缺陷严重程度：**
- `Blocker`（🔴 阻塞级）
- `Critical`（⛔ 严重）
- `Major`（🟠 主要）
- `Minor`（🟡 次要）

**状态流转：**
```
打开(open) → 进行中(in_progress) → 已修复(fixed) → 已关闭(closed) / 不修复(wont_fix)
```

### 8.4 项目管理

- 项目 CRUD：创建/查看/更新/删除
- 语言标注：Python/Java/JavaScript/Go
- 项目状态：active/archived

### 8.5 测试洞察（一线测试人员功能）

| 功能 | API | 说明 |
|------|-----|------|
| 价值量化 | `GET /api/insights/value` | 缺陷价值分、覆盖率可视化、避免事故估算 |
| 执行追溯 | `GET/POST /api/insights/trace` | 每次测试留痕、一键"自证清白" |
| 风险预警 | `GET /api/insights/risk` | 代码复杂度+覆盖率缺口+缺陷密度风险分 |
| 低代码生成 | `POST /api/insights/lowcode` | 自然语言描述 → 自动生成 pytest |
| 技能阶梯 | `GET /api/insights/skill-path` | 功能→自动化→测试开发职业进阶 |

---

## 九、接口测试模块

### 9.1 功能概览

集成 TestPilot 风格的接口测试能力：

| 功能 | 说明 |
|------|------|
| 接口定义管理 | HTTP/TCP/SQL/DUBBO 协议 |
| 接口用例管理 | 断言/前置后置脚本/变量提取 |
| 场景编排 | 多接口按序执行，支持逻辑控制器 |
| Mock 服务 | 模拟接口响应，支持延迟与自定义 |
| 断言规则 | 文本/正则/JSONPath/XPath/状态码 |
| 接口导入 | Postman Collection / Swagger |
| 环境管理 | 多环境切换（Base URL/头/变量） |
| 接口调试 | 在线调试，查看响应与提取变量 |

### 9.2 核心 API

| 功能 | API |
|------|-----|
| 接口定义 CRUD | `GET/POST /api/apitest/definitions` |
| 接口用例 CRUD | `GET/POST /api/apitest/cases` |
| 场景编排 | `POST /api/apitest/scenarios/{id}/execute` |
| Mock 服务 | `GET/POST /api/apitest/mocks` |
| 接口导入 | `POST /api/apitest/import` |
| 环境管理 | `GET/POST /api/apitest/environments` |
| 接口调试 | `POST /api/apitest/debug` |

### 9.3 使用示例（curl）

```bash
# 创建接口定义
curl -X POST http://localhost:8000/api/apitest/definitions \
  -H "Content-Type: application/json" \
  -d '{
    "name": "获取用户信息",
    "method": "GET",
    "path": "/api/users/{id}",
    "protocol": "HTTP"
  }'

# 创建接口用例
curl -X POST http://localhost:8000/api/apitest/cases \
  -H "Content-Type: application/json" \
  -d '{
    "name": "正常获取用户",
    "definition_id": "<definition_id>",
    "assertions": [{"type": "status_code", "expected": 200}]
  }'

# 执行接口调试
curl -X POST http://localhost:8000/api/apitest/debug \
  -H "Content-Type: application/json" \
  -d '{"method": "GET", "url": "https://api.example.com/users/1"}'
```

---

## 十、测试洞察与数据管理

### 10.1 价值量化

```bash
# 查看测试价值汇总
curl http://localhost:8000/api/insights/value
```

返回包含：
- **缺陷价值分**：按严重度折算的"发现缺陷/避免线上事故"价值
- **覆盖率可视化**：汇总平均覆盖率
- **避免事故估算**：已修复 blocker/critical 缺陷折算

### 10.2 执行追溯（自证清白）

```bash
# 查看执行记录
curl "http://localhost:8000/api/insights/trace?file_path=wechat_upload.py"

# 一键证明某文件的测试覆盖情况
curl "http://localhost:8000/api/insights/trace/prove?file_path=wechat_upload.py"

# 手动补记一次执行（需登录，CI 里跑完测试后回写，追溯链才完整）
# 1) 登录拿 token
TOKEN=$(curl -s -X POST http://localhost:8000/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}' \
  | python3 -c "import sys,json; print(json.load(sys.stdin)['data']['token'])")

# 2) 带 token 回写执行记录
curl -X POST http://localhost:8000/api/insights/trace \
  -H "Content-Type: application/json" \
  -H "X-AUTH-TOKEN: $TOKEN" \
  -d '{"file_path":"app/defects/tracker.py","result":"passed","passed_count":12,"coverage":85,"attribution":"code_regression"}'
```

### 10.2.1 线上事故免责自查 SOP

被问"为什么没测出来"时，按下面四步取证，避免靠记忆和口头解释：

1. **取证据**：`GET /api/insights/trace/prove?file_path=<出问题的文件>`
   - `has_passed_evidence=true` → 当时确实测过并通过了
   - `last_run.attribution` 说明为什么现在出问题
2. **看归因**：`requirement_change` 需求变更 / `environment_anomaly` 环境异常 /
   `data_issue` 数据问题 / `code_regression` 代码回归 / `coverage_gap` 覆盖遗漏
3. **定动作**：

   | 归因 | 处理动作 |
   |------|---------|
   | 需求变更 | 补用例，同步需求变更记录 |
   | 环境异常 | 修环境/基线，重跑 |
   | 数据问题 | 修数据工厂种子数据 |
   | 代码回归 | 提缺陷，纳入回归集 |
   | 覆盖遗漏 | 补测并回填覆盖率 |

4. **留痕**：把 `prove` 返回的 JSON 附到事故复盘里，作为"当时测过"的客观证据。

> 前提：每次执行都要回写追溯记录（`POST /api/insights/trace`，需 `X-AUTH-TOKEN`），
> 否则 `prove` 查不到数据，免责链就断了。`GET /api/insights/trace/prove` 为公开接口，取证时无需登录。

### 10.3 风险预警

```bash
# 评估项目风险
curl "http://localhost:8000/api/insights/risk?project_path=./src"
```

风险分 = 复杂度分(0-40) + 覆盖率缺口分(0-30) + 缺陷密度分(0-30)，
输出 `high_risk` / `medium_risk` / `low_risk` 三档清单（默认阈值 60 / 30）。

### 10.3.1 用作发布门禁

高风险模块没回归就不放行，可直接用退出码阻断流水线：

```bash
curl -s "http://localhost:8000/api/insights/risk?project_path=./src" \
  | python3 -c "import sys,json; d=json.load(sys.stdin)['data']; \
print('high:', [m['file_path'] for m in d['high_risk']]); \
sys.exit(1 if d['high_risk'] else 0)"
```

比单看总覆盖率更准：整体覆盖率达标，但某个高风险模块没回归，依然会拦下来。

### 10.4 低代码生成

```bash
# 用自然语言描述测试意图
curl -X POST http://localhost:8000/api/insights/lowcode \
  -H "Content-Type: application/json" \
  -d '{"intent": "给我生成一个登录接口的测试"}'
```

### 10.5 技能阶梯

```bash
curl http://localhost:8000/api/insights/skill-path
```

---

## 十一、项目扫描与批量生成

### 11.1 扫描项目

```bash
# 通过 API 扫描
curl -X POST http://localhost:8000/api/projects/scan \
  -H "Content-Type: application/json" \
  -d '{"path": "./src"}'
```

扫描结果包括：
- 文件列表（路径、相对路径、大小）
- 每个文件的函数签名
- 文件/函数总数统计

### 11.2 批量生成

```bash
# 通过 API 批量生成
curl -X POST http://localhost:8000/api/projects/generate \
  -H "Content-Type: application/json" \
  -d '{"path": "./src", "test_type": "functional"}'
```

### 11.3 排除规则

扫描时自动排除以下目录/文件：
- `.git`, `__pycache__`, `venv`, `.venv`, `node_modules`
- 文件名以 `test_` 开头或以 `_test` 结尾的文件

---

## 十二、报告中心

### 12.1 支持的报告格式

| 格式 | 用途 |
|------|------|
| HTML | 在线预览，可视化展示 |
| JUnit XML | CI/CD 集成 |
| Markdown | 文档协作 |

### 12.2 生成报告

```bash
# Web 控制台
# 在「报告中心」页面点击「生成报告」

# CLI
python -m app.cli generate --source-file demo.py --report --format html
python -m app.cli generate --source-file demo.py --report --format junit
python -m app.cli generate --source-file demo.py --report --format markdown

# API
curl -X POST http://localhost:8000/api/reports/generate \
  -H "Content-Type: application/json" \
  -d '{"format": "html", "project_name": "my-project"}'
```

### 12.3 管理报告

```bash
# 列出报告
curl http://localhost:8000/api/reports/list

# 下载报告
curl http://localhost:8000/api/reports/download/<filename>

# 移至回收站
curl -X POST http://localhost:8000/api/reports/<filename>/trash

# 恢复
curl -X POST http://localhost:8000/api/reports/trash/<filename>/restore

# 彻底删除
curl -X DELETE http://localhost:8000/api/reports/trash/<filename>
```

### 12.4 报告内容

生成的报告包含：
- 项目名与生成时间
- 每个文件的测试结果（通过/失败）
- 覆盖率统计
- 重试次数
- 失败原因分析

---

## 十三、异步任务与 WebSocket

### 13.1 异步任务队列

对于耗时较长的生成任务，系统支持异步执行：

```bash
# 提交异步任务
curl -X POST http://localhost:8000/api/tasks \
  -H "Content-Type: application/json" \
  -d '{"type": "generate", "params": {"source_code": "..."}}'

# 查询任务状态
curl http://localhost:8000/api/tasks/<task_id>

# 列出所有任务
curl http://localhost:8000/api/tasks
```

### 13.2 WebSocket 流式生成

```javascript
// 浏览器端
const ws = new WebSocket('ws://localhost:8000/ws/generate');
ws.onmessage = (event) => {
  const data = JSON.parse(event.data);
  // 处理实时进度
};
ws.send(JSON.stringify({
  source_code: "...",
  test_type: "functional"
}));
```

---

## 十四、Docker 沙箱隔离

### 14.1 工作原理

测试代码在隔离的 Docker 容器中运行：
- 文件系统隔离
- 网络禁用（默认）
- CPU/内存资源限制
- 自动清理临时容器

### 14.2 配置

```ini
SANDBOX_ENABLED=true
SANDBOX_IMAGE=python:3.12-slim
SANDBOX_MEM_LIMIT=512m
SANDBOX_NANO_CPUS=1000000000
SANDBOX_NETWORK_MODE=none
```

### 14.3 降级行为

当 Docker 不可用时（未安装/无权限），系统自动降级为**宿主机子进程执行**，不影响功能使用。

---

## 十五、性能测试引擎

### 15.1 触发条件

默认在以下条件满足时自动触发：
1. 测试全部通过
2. 覆盖率达标（≥ `COVERAGE_THRESHOLD`）
3. `PERF_ENABLED=true`

### 15.2 SLO 校验指标

| 指标 | 默认阈值 |
|------|----------|
| P95 响应时间 | ≤ 1 秒 |
| 平均响应时间 | ≤ 500ms |
| 吞吐量 | ≥ 10 次/秒 |
| 峰值内存 | ≤ 512MB |

### 15.3 手动触发

```bash
python -m app.cli generate --source-file demo.py --test-type performance
```

---

## 十六、前端开发与构建

### 16.1 开发环境

```bash
cd frontend
# 安装依赖（TestPilot 官方工程存在 peer 冲突）
npm install --legacy-peer-deps --no-audit --no-fund
# 启动 dev server（默认 5173，/front 和 /api 代理到后端 8000）
npm run dev
```

### 16.2 构建生产产物

```bash
npm run build
# 等价于 npx vite build --config ./config/vite.config.prod.ts
```

产物输出到 `frontend/dist/`。

### 16.3 前端路由模块

| 路由前缀 | 模块 |
|----------|------|
| `/ai-case-gen` | AI 用例生成 |
| `/api-test` | 接口测试 |
| `/bug-management` | 缺陷管理 |
| `/case-management` | 用例管理 |
| `/project-management` | 项目管理 |
| `/setting` | 系统设置 |
| `/test-plan` | 测试计划 |
| `/workstation` | 工作台 |

### 16.4 一键开发脚本

```bash
./scripts/dev-frontend.sh            # 安装依赖并同时启动前后端
./scripts/dev-frontend.sh setup      # 仅安装依赖
./scripts/dev-frontend.sh backend    # 仅启动后端
./scripts/dev-frontend.sh frontend   # 仅启动前端
./scripts/dev-frontend.sh check      # 仅检查环境
```

---

## 十七、测试与质量保障

### 17.1 安装测试依赖

```bash
pip install -r requirements-dev.txt
```

### 17.2 运行测试

```bash
# 运行全部测试（带覆盖率）
pytest tests/ -v --cov=app --cov-report=term-missing

# 运行特定模块
pytest tests/test_cli.py -v
pytest tests/test_mock_generator_extended.py -v
pytest tests/test_api_integration.py -v
pytest tests/test_performance_engine.py -v

# 运行 API 测试
pytest tests/test_api_adapter.py -v
pytest tests/test_api_enterprise.py -v
```

### 17.3 路由冲突检查

```bash
python3 scripts/route_conflict_check.py --check      # 检查冲突
python3 scripts/route_conflict_check.py --generate   # 生成 API 映射表
```

### 17.4 数据库合并工具

```bash
python3 scripts/merge_databases.py  # 合并旧数据库到统一 tga.db
```

### 17.5 代码规范检查（Ruff）

项目此前没有 lint 配置，未使用导入、裸 `except`、变量重定义等问题无法被拦截。
现已引入 `ruff.toml`，但**不采用全量门禁**——存量代码问题较多，一次性开启会淹没 PR。

采用**增量门禁**策略：只检查本次变更涉及的 Python 文件。

```bash
python3 scripts/lint_diff.py                    # 对比 origin/main（CI 用法）
python3 scripts/lint_diff.py --base HEAD~1      # 指定基线
python3 scripts/lint_diff.py --all              # 全量体检（不卡门禁，仅作治理参考）
```

约定：

- 变更文件的 lint 问题必须清零才能合并，新代码不允许再引入历史包袱
- 存量文件的历史问题不计入门禁，靠 `--all` 的输出逐步治理
- 基线 ref 不可用时（浅克隆/跨仓）会自动回落到 `origin/main`

---

## 十八、CI/CD 与 Docker 部署

### 18.1 CI 流水线（`.cnb.yml`）

项目已配置多架构镜像构建流水线：

| 阶段 | 说明 |
|------|------|
| docker login | 登录制品库 |
| route conflict check | 路由冲突检测 |
| docker build | 构建镜像（amd64/arm64 并行） |
| docker push | 推送镜像到制品库 |
| manifest | 合并多架构 manifest |

### 18.2 多架构镜像

- `latest-linux-amd64`：x86_64 平台
- `latest-linux-arm64`：ARM64 平台（Apple Silicon 等）
- `latest`：自动合并的多架构 manifest

### 18.3 生产部署建议

```bash
# 生产环境推荐配置
docker run -d --name test-gen-agent \
  -p 8000:8000 \
  -e OPENAI_API_KEY=<your-key> \
  -e ENVIRONMENT=production \
  -e DEBUG=false \
  -e SANDBOX_ENABLED=true \
  -v /data/test-gen-agent:/app/data \
  --restart unless-stopped \
  test-gen-agent
```

---

## 十九、常见问题排查（FAQ）

### Q1: 启动时提示 `OPENAI_API_KEY 未设置`

```
配置校验失败:
  - OPENAI_API_KEY 未设置
```

**解决：** 复制 `.env.example` 为 `.env` 并填入 `OPENAI_API_KEY`。使用本地兼容端点（如 vLLM）可填占位符。

### Q2: 测试生成失败，提示 LLM 限流

**解决：** 系统已内置限流重试机制。检查 `LLM_RATE_LIMIT_*` 配置，适当增加重试次数或调整延迟。

### Q3: Docker 沙箱无法使用

**解决：** 检查 Docker 服务是否启动。未安装 Docker 时系统会自动降级为宿主机执行，不影响基本功能。

### Q4: 前端 `npm install` 报 peer 冲突

**解决：** 必须使用 `--legacy-peer-deps` 参数：
```bash
npm install --legacy-peer-deps --no-audit --no-fund
```

### Q5: 端口被占用

```bash
# 修改端口
PORT=8001 uvicorn app.main:app --host 0.0.0.0 --port 8001
```

### Q6: 数据库文件位置

所有业务数据统一存储在项目根目录的 `tga.db`，LangGraph 检查点存储在 `checkpoints.db`。

### Q7: 前端 API 请求 404

确认访问路径带 `/front/` 前缀（Vite dev 已配置代理）或 `/ms/` 前缀（生产部署）。

### Q7.1: 打开 `/ms/xxx` 页面一片空白，或者直接显示一段 JSON

这是典型的 **SPA 回退路由被其它路由抢先命中**。

现象：浏览器访问 `/ms/workbench`，返回的是

```
Content-Type: application/json
{"code":200,"message":"success","data":{"code":200,"message":"success","data":null}}
```

HTTP 状态码是 **200**，所以只看状态码的监控/测试都发现不了。

根因：`/ms/{full_path:path}` 是 catch-all，谁注册得早谁生效。
历史上 `app/routers/system.py` 里的占位透传路由 `ms_passthrough`
因为随业务路由先 include，抢走了所有 `/ms/xxx` 请求，
于是 FastAPI 返回 JSON 而不是 `index.html`。

排障三步：

```bash
# 1. 看 content-type，html 才是对的
curl -sI http://localhost:8000/ms/workbench | grep -i content-type

# 2. 找全项目还有谁注册了 GET /ms
grep -rn -E '@[a-zA-Z_]+\.get\("?/ms' app/ --include=*.py
#   期望：只命中 app/main.py，且处理函数是 ms_frontend

# 3. 跑回归测试（产物存在时逐页面校验，产物不存在时只校验路由层）
python3 -m pytest tests/test_ms_frontend_serving.py -q
```

### Q8: 生成测试覆盖率不达标

**解决：** 增大 `MAX_COVERAGE_RETRIES`（默认 5）或降低 `COVERAGE_THRESHOLD`（默认 80.0）。

### Q9: 性能测试未触发

确认 `PERF_ENABLED=true`，且测试通过 + 覆盖率达标。

### Q10: WebSocket 无法连接

确保访问路径使用 `/ws/generate`，且在 Nginx 中配置了 WebSocket 升级头。

---

## 二十、API 参考

### 20.1 健康检查

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/health` | 健康检查 |

### 20.2 认证

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/login` | 登录（RSA 加密密码） |
| GET | `/is-login` | 检查登录状态 |
| POST | `/signout` | 登出 |
| GET | `/get-key` | 获取 RSA 公钥 |

### 20.3 测试生成

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/test-types` | 列出测试类型 |
| POST | `/api/generate` | 生成测试（`?async_mode=true`） |
| POST | `/api/generate/structured` | 生成结构化用例 |
| POST | `/api/tasks` | 提交异步任务 |
| GET | `/api/tasks/{id}` | 查询任务状态 |
| GET | `/api/tasks` | 列出任务 |
| WS | `/ws/generate` | WebSocket 流式生成 |

### 20.4 用例管理

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/cases` | 列出用例（支持过滤） |
| POST | `/api/cases` | 创建用例 |
| GET | `/api/cases/{id}/full` | 获取用例完整信息 |
| PUT | `/api/cases/{id}` | 更新用例 |
| DELETE | `/api/cases/{id}` | 删除用例 |
| GET | `/api/cases/stats` | 用例统计 |
| GET | `/api/cases/mindmap` | 用例脑图 |
| GET | `/api/cases/export` | 导出（excel/mindmap） |
| POST | `/api/cases/import` | 导入 |
| POST | `/api/cases/{id}/reviews/submit` | 提交评审 |
| POST | `/api/cases/{id}/reviews/approve` | 通过评审 |
| POST | `/api/cases/{id}/reviews/reject` | 驳回评审 |
| POST/GET/DELETE | `/api/cases/{id}/relations` | 用例关联 |
| POST/GET/DELETE | `/api/cases/{id}/dependencies` | 依赖管理 |
| GET | `/api/cases/trash` | 回收站列表 |
| POST | `/api/cases/{id}/trash` | 软删除 |
| POST | `/api/cases/{id}/restore` | 恢复 |
| DELETE | `/api/cases/{id}/purge` | 彻底删除 |
| GET | `/api/cases/{id}/versions` | 版本列表 |
| POST | `/api/cases/{id}/rollback` | 版本回滚 |
| GET | `/api/cases/{id}/changes` | 变更记录 |
| POST/GET/DELETE | `/api/cases/{id}/requirements` | 需求关联 |

### 20.5 缺陷管理

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/defects` | 列出缺陷 |
| POST | `/api/defects` | 创建缺陷 |
| GET | `/api/defects/{id}` | 获取缺陷 |
| PUT | `/api/defects/{id}` | 更新缺陷 |
| DELETE | `/api/defects/{id}` | 删除缺陷 |

### 20.6 项目管理

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/projects` | 列出项目 |
| POST | `/api/projects` | 创建项目 |
| GET | `/api/projects/{id}` | 获取项目 |
| PUT | `/api/projects/{id}` | 更新项目 |
| DELETE | `/api/projects/{id}` | 删除项目 |
| POST | `/api/projects/scan` | 扫描项目 |
| POST | `/api/projects/generate` | 项目级生成 |

### 20.7 接口测试（apitest）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/apitest/stats` | 接口测试统计 |
| GET/POST | `/api/apitest/definitions` | 接口定义 CRUD |
| GET/POST | `/api/apitest/cases` | 接口用例 CRUD |
| GET/POST | `/api/apitest/scenarios` | 场景编排 |
| GET/POST | `/api/apitest/mocks` | Mock 服务 |
| POST | `/api/apitest/import` | 接口导入 |
| GET/POST | `/api/apitest/environments` | 环境管理 |
| POST | `/api/apitest/debug` | 接口调试 |

### 20.8 报告管理

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/reports/generate` | 生成报告 |
| GET | `/api/reports/list` | 列出报告 |
| GET | `/api/reports/download/{name}` | 下载报告 |
| POST | `/api/reports/{name}/trash` | 移至回收站 |
| GET | `/api/reports/trash/list` | 回收站列表 |
| POST | `/api/reports/trash/{name}/restore` | 恢复报告 |
| DELETE | `/api/reports/trash/{name}` | 彻底删除 |

### 20.9 测试洞察

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/insights/value` | 价值量化 |
| GET/POST | `/api/insights/trace` | 执行追溯 |
| GET | `/api/insights/trace/prove` | 自证清白 |
| GET | `/api/insights/risk` | 风险预警 |
| POST | `/api/insights/lowcode` | 低代码生成 |
| GET | `/api/insights/skill-path` | 技能阶梯 |

### 20.10 测试计划

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/test-plan/page` | 测试计划分页查询 |
| POST | `/test-plan/add` | 创建测试计划 |
| POST | `/test-plan/update` | 更新测试计划 |
| POST | `/test-plan/delete` | 删除测试计划 |
| POST | `/test-plan/association/add` | 关联用例 |
| GET | `/test-plan/statistics/{plan_id}` | 计划统计 |

---

> **提示**：完整的 API 文档可通过访问 `http://localhost:8000/docs`（Swagger UI）查看，包含所有路由的参数定义与请求/响应格式。
