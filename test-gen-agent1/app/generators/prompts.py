"""
测试类型 Prompt 模板库
======================
按行业测试类型分类，为 LLM 提供针对性的测试生成指令。

每种类型的 Prompt 都引导 LLM 生成符合该类型特征、可运行的 pytest 测试。
同时支持「结构化用例 + 可选脚本」的生成模式（方案A）。
"""

from typing import Dict

# ════════════════════════════════════════════════════════════
# 0. 结构化用例输出模板（方案A）
# ════════════════════════════════════════════════════════════

STRUCTURED_OUTPUT_FORMAT = '''
## 输出格式（严格遵循）

请按以下 JSON 数组格式返回结构化测试用例（不要返回 markdown 代码块，只返回纯 JSON）：

```json
[
  {
    "title": "用例标题",
    "description": "用例描述",
    "test_type": "{test_type_key}",
    "preconditions": ["前置条件1", "前置条件2"],
    "test_steps": [
      {"step": "操作步骤描述", "data": "测试数据", "expected": "预期结果"}
    ],
    "test_data": {"key": "value"},
    "priority": "P0/P1/P2/P3",
    "tags": ["标签1", "标签2"],
    "risk_level": "high/medium/low",
    "execution_type": "manual/automated"
  }
]
```

**JSON 字段说明**：
- `title`: 简洁明确的用例标题
- `preconditions`: 执行前置条件（数组）
- `test_steps`: 操作步骤列表，每步含 step（操作）/ data（数据）/ expected（预期结果）
- `test_data`: 测试数据对象
- `priority`: 优先级 P0-P3
- `risk_level`: 风险等级
- `execution_type`: 执行方式 manual（手工）/ automated（自动化）
'''


# ════════════════════════════════════════════════════════════
# 1. 通用测试模板前缀（所有类型共用）
# ════════════════════════════════════════════════════════════

_BASE_PROMPT = '''你是资深测试工程师。根据以下函数签名生成测试用例。

## 被测函数签名
{signatures}

## Mock 配置提示
{mocks}

''' + STRUCTURED_OUTPUT_FORMAT


# ════════════════════════════════════════════════════════════
# 2. 各类型专属 Prompt 模板
# ════════════════════════════════════════════════════════════

PROMPTS: Dict[str, str] = {
    # ── 功能测试 ──────────────────────────────────────────
    "functional": _BASE_PROMPT + '''

## 本测试类型：功能测试

### 测试重点
- **正常流程**：用类型正确的参数验证函数返回预期结果
- **边界条件**：空值 / 0 / None / 极值 / 最大整数 / 空字符串 / 空列表
- **异常处理**：非法输入应抛出正确异常类型
- **回归保护**：覆盖关键路径防止回归

### 必须覆盖
- 至少 1 个正向测试
- 至少 2 个边界测试
- 至少 2 个异常测试

### 结构化要求
- `test_steps` 中每步都要明确「操作 + 数据 + 预期」
- 功能测试聚焦业务行为，不涉及具体实现细节
- `execution_type` 默认填 "manual"
''',

    # ── 接口测试 ──────────────────────────────────────────
    "api": _BASE_PROMPT + '''

## 本测试类型：接口测试

### 测试重点
- **请求契约**：验证请求参数的类型、必填项、格式
- **响应契约**：验证返回值类型、数据结构、字段完整性
- **状态码/异常**：验证错误场景下的返回行为
- **参数校验**：验证非法参数 / 缺失参数 / 错误类型的处理
- **数据格式**：验证返回 JSON/列表/字典等数据结构的正确性

### 必须覆盖
- 至少 2 个正常请求-响应测试
- 至少 2 个参数异常测试（缺失参数/非法格式）
- 至少 1 个极端输入测试（超大值/空值/特殊字符）

### 结构化要求
- `test_steps` 每步描述「发送请求 → 验证响应」的完整链路
- 在 `test_data` 中明确请求参数、期望状态码、期望响应结构
- `execution_type` 默认填 "automated"
- 可补充 `api_info`: {"method": "GET/POST", "path": "/api/xxx", "headers": {}}
''',

    # ── UI 测试 ───────────────────────────────────────────
    "ui": _BASE_PROMPT + '''

## 本测试类型：UI 测试

### 测试重点
- **元素定位**：验证页面关键元素的可见性 / 存在性
- **交互流程**：验证用户操作的完整链路（点击/输入/导航）
- **状态变化**：验证交互后的 UI 状态（禁用/加载/错误提示）
- **数据展示**：验证页面数据渲染的正确性和完整性
- **响应式**：验证不同视口下的布局（如适用）

### 必须覆盖
- 至少 2 个关键元素可见性测试
- 至少 2 个交互流程测试
- 至少 1 个异常/边界状态测试（如空数据、错误提示）

### 结构化要求
- `test_steps` 每步描述「操作 + 元素定位 + 预期UI状态」
- 在 `test_data` 中可补充 `ui_selectors`: {"按钮": "#submit-btn", "输入框": "input[name=user]"}
- `execution_type` 默认填 "manual"
''',

    # ── 性能测试 ──────────────────────────────────────────
    "performance": _BASE_PROMPT + '''

## 本测试类型：性能测试

### 测试重点
- **响应时间**：验证单次调用耗时在预期范围内
- **吞吐量**：验证单位时间内的处理能力
- **资源消耗**：验证内存 / CPU 使用在合理范围内
- **并发能力**：验证多线程/多请求场景下的稳定性
- **基准对比**：为关键路径建立性能基线

### 必须覆盖
- 至少 1 个响应时间基准测试
- 至少 1 个吞吐量测试
- 至少 1 个并发场景测试（如适用）

### 结构化要求
- `test_data` 中明确性能指标：`{"concurrency": 10, "duration_sec": 30, "p95_ms": 1000, "throughput_rps": 100}`
- `test_steps` 每步描述「负载场景 + 采样指标」
- `execution_type` 默认填 "automated"
''',

    # ── 安全测试 ──────────────────────────────────────────
    "security": _BASE_PROMPT + '''

## 本测试类型：安全测试

### 测试重点
- **输入校验**：验证 SQL 注入 / XSS / 命令注入防护
- **越权访问**：验证未授权访问被拒绝
- **路径穿越**：验证文件路径访问边界
- **敏感信息**：验证不泄漏密码/密钥/个人信息
- **权限控制**：验证角色权限边界
- **DoS 防护**：验证超大输入不会导致崩溃

### 必须覆盖
- 至少 2 个注入/恶意输入测试
- 至少 1 个越权/权限边界测试
- 至少 1 个敏感信息保护测试

### 结构化要求
- `risk_level` 明确标出高风险/中风险/低风险
- `test_data` 中明确攻击载荷：`{"attack_payload": "'; DROP TABLE--", "attack_type": "SQL注入"}`
- `test_steps` 每步描述「攻击输入 + 预期防护结果」
- `execution_type` 默认填 "automated"
''',

    # ── 兼容性测试 ────────────────────────────────────────
    "compatibility": _BASE_PROMPT + '''

## 本测试类型：兼容性测试

### 测试重点
- **版本兼容**：验证不同 Python 版本/依赖版本下行为一致
- **平台兼容**：验证不同操作系统/环境的兼容性
- **接口兼容**：验证 API 签名变化时的向后兼容
- **数据兼容**：验证旧格式数据仍可正常处理
- **配置兼容**：验证不同配置项组合下的正确性

### 必须覆盖
- 至少 2 个不同参数/配置组合的兼容性测试
- 至少 1 个旧格式数据兼容测试（如适用）
- 至少 1 个边界配置值测试

### 结构化要求
- `test_data` 中明确兼容矩阵：`{"compat_matrix": [{"python_ver": "3.9"}, {"python_ver": "3.12"}]}`
- `test_steps` 每步描述「环境/版本条件 + 验证行为」
- `execution_type` 默认填 "automated"
''',

    # ── 可靠性测试 ────────────────────────────────────────
    "reliability": _BASE_PROMPT + '''

## 本测试类型：可靠性测试

### 测试重点
- **幂等性**：验证重复执行结果一致
- **容错性**：验证部分失败时的降级/恢复行为
- **超时处理**：验证超时场景不会挂起
- **资源释放**：验证异常后资源被正确释放
- **状态恢复**：验证异常后状态可恢复
- **长时间运行**：验证长时间运行无内存泄漏

### 必须覆盖
- 至少 1 个幂等性测试（重复调用结果一致）
- 至少 1 个容错测试（mock 抛异常时优雅处理）
- 至少 1 个超时/资源释放测试（如适用）

### 结构化要求
- `test_data` 中明确故障场景：`{"failure_scenario": "网络超时", "retry_times": 3}`
- `test_steps` 每步描述「故障注入 + 降级行为 + 恢复验证」
- `execution_type` 默认填 "automated"
''',
}


# ════════════════════════════════════════════════════════════
# 3. 从结构化用例生成 pytest 脚本的 Prompt
# ════════════════════════════════════════════════════════════

SCRIPT_GENERATION_PROMPT = '''你是资深测试工程师。根据以下【结构化测试用例】生成完整、可运行的 pytest 测试代码。

## 被测函数签名
{signatures}

## Mock 配置提示
{mocks}

## 结构化测试用例（JSON）
{structured_cases}

## 代码生成要求
1. 为每个结构化用例生成对应的测试方法，命名 `test_{{场景}}_{{期望结果}}`
2. 每个测试方法必须包含 docstring 和 Arrange/Act/Assert 三段式注释
3. 外部依赖（DB/HTTP/云SDK/文件/时间）必须使用 `@patch` 模拟
4. Mock 外部依赖时必须设置明确的 return_value
5. 代码必须严格遵循 PEP8，可运行无语法错误
6. 每个测试方法的断言应尽量反映结构化用例中的 `expected` 预期

请**只返回完整可运行的测试代码**（不含解释文字、不含 markdown 代码块围栏）。'''


def get_prompt(test_type: str) -> str:
    """
    获取指定测试类型的 Prompt 模板。

    Args:
        test_type: 测试类型 key，未知类型回退到功能测试

    Returns:
        Prompt 模板字符串
    """
    from app.generators.test_types import get_default_test_type
    return PROMPTS.get(test_type, PROMPTS[get_default_test_type()])


def get_script_generation_prompt() -> str:
    """
    获取从结构化用例生成 pytest 脚本的 Prompt 模板。
    """
    return SCRIPT_GENERATION_PROMPT
